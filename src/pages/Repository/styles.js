import styled from 'styled-components/native';
import { WebView } from 'react-native-webview';
//WebView -> componente que integra `a aplicação uma pagina url 
export const Container = styled(WebView)`
    flex: 1;
`;
